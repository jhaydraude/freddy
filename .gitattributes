# Enforce LF for shell scripts to ensure they run on Linux
*.sh text eol=lf
*.bash text eol=lf

# Enforce LF for Dockerfiles and YAML configuration
Dockerfile* text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# All other files use default (auto-detect)
* text=auto
